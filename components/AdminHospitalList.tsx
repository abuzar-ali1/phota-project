"use client";
import { BadgeCheck, Ban, Building2, LoaderCircle, MapPin, ShieldCheck } from "lucide-react";
import { useState } from "react";
import type { HospitalProfile } from "@/lib/types";

export function AdminHospitalList({initialHospitals}:{initialHospitals:HospitalProfile[]}){const[hospitals,setHospitals]=useState(initialHospitals);const[busy,setBusy]=useState<string|null>(null);const[message,setMessage]=useState("");async function review(id:string,status:"verified"|"rejected"){setBusy(id);setMessage("");try{const response=await fetch(`/api/admin/hospitals/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})});const result=await response.json();if(!response.ok)throw new Error(result.error);setHospitals((current)=>current.map((h)=>h.id===id?result.hospital:h));setMessage(`Hospital ${status}.`);}catch(e){setMessage(e instanceof Error?e.message:"Review failed.");}finally{setBusy(null);}}return <div className="space-y-4">{message&&<p role="status" className="rounded-xl border border-emerald-300/15 bg-emerald-300/[.06] p-3 text-sm text-emerald-200">{message}</p>}{hospitals.map((h)=><article key={h.id} className="rounded-2xl border border-white/8 bg-white/[.03] p-5 transition hover:bg-white/[.045]"><div className="flex flex-col gap-5 lg:flex-row lg:items-center lg:justify-between"><div className="flex gap-4"><span className="grid size-12 shrink-0 place-items-center rounded-xl bg-cyan-300/10 text-cyan-300"><Building2/></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-semibold text-white">{h.hospitalName}</h2><span className={`status-${h.verificationStatus}`}>{h.verificationStatus}</span></div><p className="mt-1 text-sm text-slate-400">{h.hospitalType} · {h.licenseNumber}</p><p className="mt-2 flex items-center gap-1 text-xs text-slate-500"><MapPin className="size-3.5"/>{h.address}, {h.city}</p><p className="mt-1 text-xs text-slate-500">{h.officialEmail} · {h.phone} · Administrator: {h.adminName}</p></div></div><div className="flex gap-2 lg:shrink-0"><button disabled={busy===h.id} onClick={()=>void review(h.id,"verified")} className="secondary-btn border-emerald-300/20 text-emerald-200">{busy===h.id?<LoaderCircle className="animate-spin"/>:<BadgeCheck/>} Verify</button><button disabled={busy===h.id} onClick={()=>void review(h.id,"rejected")} className="secondary-btn border-rose-300/20 text-rose-200"><Ban/> Reject</button></div></div></article>)}{!hospitals.length&&<div className="rounded-2xl border border-white/8 bg-white/[.03] p-12 text-center text-slate-400"><ShieldCheck className="mx-auto mb-3 size-8 text-emerald-300"/>No hospital applications yet.</div>}</div>}

